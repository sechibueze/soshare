import {
  getAuth,
  createUserWithEmailAndPassword,
  signOut,
} from 'firebase/auth';
import app from './firebase.config';

export const createUser = async (params: type) => {};
export const loginUser = async (params: type) => {};
export const logoutUser = async (params: type) => {};
